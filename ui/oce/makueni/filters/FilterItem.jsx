import translatable from '../../translatable';

class FilterItem extends translatable(React.Component) {
  constructor(props) {
    super(props);
    
  }
  
  render() {
    
    return (<div>
      
      </div>
    );
  }
}

export default FilterItem;
